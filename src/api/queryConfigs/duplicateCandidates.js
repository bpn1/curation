const queryConfig = {
  normal: ['subject_id', 'datasource', 'subject_name'],
  lists: ['candidates'],
  maps: [],
  uniqueKey: 'subject_id',
  like: ['subject_name']
};

module.exports = queryConfig;

